// Firebase Firestore Security Rules - ALLOW ALL (Development Only)
// ⚠️ WARNING: These rules allow anyone to read/write your database
// ⚠️ Only use for development/testing, NOT for production!

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes to any document
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
